import styled from 'styled-components'

export const Circle = styled.div`
  pointer-events: none;
  height: 32px;
  width: 32px;
  border-radius: 50%;
  background-color: var(--c-white);
  z-index: 5;
`
